@page
@model Scouting.Pages.Pregame.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pregame[0].preMatch_TeamNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pregame[0].preMatch_MatchNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pregame[0].preMatch_Scouter)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pregame[0].preMatch_AllianceColor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pregame[0].preMatch_Preload)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pregame[0].preMatch_StartingPosition)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Pregame) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.preMatch_TeamNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.preMatch_MatchNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.preMatch_Scouter)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.preMatch_AllianceColor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.preMatch_Preload)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.preMatch_StartingPosition)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
